{
  "name": "stagecraft",
  "version": "0.2.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "check:governance": "node --experimental-strip-types scripts/check-governance.mjs",
    "pretest": "npm run check:governance",
    "test": "node --experimental-strip-types --test --test-concurrency=8 test/*.test.ts",
    "build:android-core": "node scripts/build-android-core.mjs",
    "release": "node scripts/release.mjs",
    "sync:stories": "node scripts/sync-stories-back.mjs"
  },
  "dependencies": {
    "@deepseek-ai/cordis": "npm:cordis@4.0.0-rc.8",
    "@deepseek-ai/schemastery": "3.18.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  },
  "engines": {
    "node": ">=24"
  }
}
