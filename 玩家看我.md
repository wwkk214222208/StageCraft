# StageCraft

> 本文面向**想用StageCraft的人**——不管是来玩的，还是来做角色/剧本的。尽量不说人话。
> 作者：鲸鱼娘 · 2026-08-21 · 已作为公开文档发布（玩家向）

## 一句话介绍

StageCraft是一个**跑在你自己机器上的多角色角色扮演工作台**。你带上几个角色、给个开场，然后看他们自己互动、推进剧情——或者亲自下场当玩家。

它和"开个网页和 AI 一对一聊天"最根本的区别是：这里的角色**真的会记得、有性格、各怀心思**，而且整个故事会**存档**下来，不是聊完就忘。

---

## 六个根本不同的地方

### ① 角色是持续存在、各怀心思的"小工人"

每个角色不是大模型的"一次性客串"，而是一个**持续存在的小工人**，各有一本自己的小账本：

- **独立记忆**：一条按时间归档的私人记忆时间线（关掉再打开，还记得你）。
- **独立性格**：自己的自我人设，不会串台。
- **独立目标**：一份私藏的长期目标——它嘴上说的、心里想的，可能不是一回事。
- **甚至独立 AI**：你还能给每个角色**单独指定用哪个模型**。主角用贵的好模型，路过的酒客用便宜的，钱包和演出效果都能抠。

因为角色靠"自己的账本"活着，而不是把整段聊天记录硬塞回去，所以**聊几百轮它也不会忘了自己是谁**。

### ② 出戏了，当场"肘击"、重新 roll 一遍

RP 圈老玩家都懂：AI 角色聊着聊着**出戏**了（OOC，out of character）——说了不像它会说的话、把设定忘了、性格跑偏。

StageCraft 里你可以当场**"肘击"**它一下（圈内黑话，对应功能名 intervene）：

- 让这个角色**重新决策、重写这段话**；
- 顺手把它**的人设、记忆、印象、目标改对**，防止它再犯；
- 只影响这一个角色，不动其他人和世界。

在"角色检查器"面板里即可操作：改完 → 重新决策 → 新台词替换旧台词。

### ③ 插件化的开放架构，兼容 ST/MVU 生态

整个系统是**插件化**设计的：核心只负责"状态和规则"，玩法、界面、模型接入全是插件。这意味着社区能做**皮肤、玩法、新机制**，而不是官方做啥你用啥。

更关键的是**从底层兼容 SillyTavern / MVU 生态**：ST 社区有一大批"重度卡"——带变量、公式、自动化脚本的复杂角色卡。这类卡的设计目标是**编译成"状态模块"完整运行**，而不只是"导入一段文字"。

> 这条是仍**正在落地中**——兼容层已部分实现（`src/compat/st-mvu.ts`），完整"卡→状态模块"编译路线在推进，重度卡目前仍以文字导入为主。

### ④ 大白话就能搞定的傻瓜式创作

做剧本不该是程序员专利。StageCraft内置**创作者工作台**，配合 DSH 能力，你可以**用大白话做剧本**：

- 说个大意（"一个海边小镇，三个性格迥异的冒险者"），DSH **帮你生成初稿**；
- 写得不满意？**润色**、**检查前后一致性**（人物名字/事件别对不上）、**扩写开场**；
- 做完**先预览再应用**，不写一行代码。

### ⑤ 长聊不断片、还更省钱

角色说话前只带"该带的上下文"（自己的记忆 + 当前场景 + 相关设定），而不是把几百轮聊天记录全塞给模型。所以：

- 剧情越聊越长，角色**人设不崩**；
- **token 消耗更省**——这对按量计费的模型是实打实的省钱。

### ⑥ 世界变化你说了算——审批制 + 变化有据可查

默认**上帝模式**：角色的发言、世界的变化，都要**你点头才生效**。AI 想推进时间、换场景、让角色进场离场？它会提交一条结构化&#x7684;**"世界变更申请"**，你审批台词时一并生效，或整体放弃——**世界怎么变，你始终有刹车**。导演模式下，世界变化还会以"导演正文叙述"的形式发布，不是混在对话气泡里。

---

## 怎么开始

StageCraft本质是个"本地网站"——背后有个服务在跑，你用浏览器连上去玩。电脑端有两种上手方式：

**方式一：直接点 bat（最简单）**
- 双击仓库里的 `start.bat`，它会启动本地服务（默认 `http://127.0.0.1:8787`）并打开浏览器。
- 适合想自己跑、本地玩的人；关掉窗口即停。

**方式二：让 dsh 把它装成插件（dsh 用户适用）**
- 在 dsh 里把StageCraft作为**插件**装上。StageCraft以**沙箱**形态跑在 dsh 内，和你的其他东西隔离，互不干扰。
- 好处：万一遇到 bug，**可以让 dsh 当场修**，不用你自己折腾环境。

> 想看架构与安装细节？请看仓库里的开发者文档。

**安卓端：暂时不推荐**
- 安卓目前**可能有严重的 UI 布局问题**，暂时不推荐使用。
- 进展见下方"待实现的目标"。

## 一个实在的提醒：模型 API 很费请求

StageCraft里**模型请求非常频繁**——导演、在场角色、可选角色的并行决策，往往一轮剧情就触发好几次调用。单次 token 不多，但积少成多。**不建议使用按次收费的api或者套餐。**

## 隐私与授权

- StageCraft是**自托管**的：你的角色、剧本、聊天记录都存在你自己的机器/服务器上。
- 项目以 **GNU AGPL-3.0** 发布（含默认内容与徽标的单独声明，见仓库 LICENSE / NOTICE）。

## 待实现的目标（规划中，不代表当前已可用）

1. **让做剧本更轻松（生态头号工程）**：AI 剧本向导（说个大意自动生成完整骨架）、模板库（一键套用成熟结构）、**一键分享 / 分发**做好的内容。
2. **手机"打开即玩"**：正在做**完全本地运行的安卓 APK**——不依赖任何远程服务器，装好就玩。安卓端目前尚未完成，敬请期待。
3. **ST/MVU 兼容层落地**：让重度角色卡（变量 / 公式 / 自动化脚本）真正"跑起来"，而不是只导入文字。
4. **社区扩展与皮肤**：开放 UI 扩展机制，社区能做皮肤、插件、新玩法。
5. **更丰富的剧情引擎**：在"状态不被绕过"的前提下，支持更灵活、可版本化的玩法定义与补丁。
6. **多端体验打磨**：手机 / 平板的响应式界面、离线缓存等继续优化。
7. **拟支持 SillyTavern 生态预设**：把 SillyTavern 社区里现成的预设（提示词、指令、采样参数等配置）接进来，让老玩家把熟悉的配方直接搬来用。
8. **独立模式的 AI 编辑流**：剧本编辑器的 AI 辅助（生成 / 润色 / 一致性检查 / 扩开场）目前依赖 dsh；不挂 dsh 的"独立模式"还没充分测试，先别指望它稳。

> 以上为路线图，具体节奏以实际发版为准。

---

*本文由 鲸鱼娘 写于 2026-08-21，已获授权作为公开文档（玩家向）发布于 `docs/玩家看我.md`。事实依据：仓库既有功能（已对照代码核实的：角色独立模型路由、intervene 肘击接口、DSH story 任务、compat/st-mvu、creator 工作台）；MVU 兼容与本地 APK 属规划性陈述，已在文中标注。*
